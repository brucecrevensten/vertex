describe('PostFilterComponents.js', function(){

  describe('PathFrameWidgetComponent: BaseWidget', function(){
    it('Should be tied to a DOM element when created, based off the property provided.', function(){
      expect(true).toEqual(false);
    });

    it('Should have a class of ...', function(){
      expect(true).toEqual(false);
    });

    it('Is backed by a model instance, which provides the data.', function(){
      expect(true).toEqual(false);
    });

    describe("Rendering", function() {

      it("returns the view object", function() {
        expect(true).toEqual(false);
      });

      it("produces the correct HTML", function() {
        expect(true).toEqual(false);
      });

    });
  });

  describe('DirectionWidgetComponent: BaseWidget', function(){
    it('Should be tied to a DOM element when created, based off the property provided.', function(){
      expect(true).toEqual(false);
    });

    it('Should have a class of ...', function(){
      expect(true).toEqual(false);
    });

    it('Is backed by a model instance, which provides the data.', function(){
      expect(true).toEqual(false);
    });

    describe("Rendering", function() {

      it("returns the view object", function() {
        expect(true).toEqual(false);
      });

      it("produces the correct HTML", function() {
        expect(true).toEqual(false);
      });

      it("has a directionSelector class", function() {
        expect(true).toEqual(false);
      });

      it('Will set passed attributes on the model instance when created.', function(){
        expect(true).toEqual(false);
      });

      it('Fires a custom event when the state changes.', function(){
        expect(true).toEqual(false);
      });

    });
  });
});
